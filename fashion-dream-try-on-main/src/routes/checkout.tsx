import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { SiteNav } from "@/components/site-nav";
import { SiteFooter } from "@/components/site-footer";
import { formatVnd } from "@/data/products";
import { useCart } from "@/lib/cart";
import { createOrder } from "@/lib/order.functions";
import { createVietQrUrl } from "@/lib/vietqr";

export const Route = createFileRoute("/checkout")({
  head: () => ({ meta: [{ title: "Thanh toán | UpThink" }, { name: "description", content: "Hoàn tất đơn hàng UpThink." }] }),
  component: CheckoutPage,
});

type PaymentInfo = { orderCode: string; total: number; qrUrl: string };

function CheckoutPage() {
  const { items, subtotal, clear } = useCart();
  const [done, setDone] = useState<string | null>(null);
  const [submitting, setSubmitting] = useState(false);
  const [paymentMethod, setPaymentMethod] = useState<"cod" | "vietqr" | "momo">("cod");
  const [paymentInfo, setPaymentInfo] = useState<PaymentInfo | null>(null);
  const shipping = subtotal === 0 || subtotal >= 1_000_000 ? 0 : 30_000;

  if (paymentInfo) return <div className="min-h-screen"><SiteNav /><main className="mx-auto max-w-2xl px-6 pb-24 pt-32 text-center sm:px-12"><p className="eyebrow">Thanh toán VietQR</p><h1 className="mt-3 text-4xl leading-tight sm:text-5xl">Quét mã để thanh toán<span className="text-primary">.</span></h1><p className="mt-4 leading-7 text-beige">Sử dụng ứng dụng ngân hàng để quét mã QR bên dưới.</p><div className="mx-auto mt-8 max-w-sm border border-border bg-card p-4 sm:p-6"><img src={paymentInfo.qrUrl} alt="Mã QR thanh toán VietQR" className="mx-auto h-auto w-full" /></div><div className="mx-auto mt-6 max-w-sm space-y-3 border border-border bg-card p-4 text-left text-sm sm:p-5"><div className="grid grid-cols-[auto_minmax(0,1fr)] gap-4"><span className="text-silver">Ngân hàng</span><span className="text-right">MB Bank</span></div><div className="grid grid-cols-[auto_minmax(0,1fr)] gap-4"><span className="text-silver">Người nhận</span><span className="text-right">UPTHINK</span></div><div className="grid grid-cols-[auto_minmax(0,1fr)] gap-4"><span className="text-silver">Số tiền</span><span className="text-right text-primary">{formatVnd(paymentInfo.total)}</span></div><div className="grid grid-cols-[auto_minmax(0,1fr)] gap-4"><span className="text-silver">Nội dung</span><span className="break-words text-right">{paymentInfo.orderCode}</span></div></div><p className="mt-6 text-xs leading-6 text-silver">Trạng thái thanh toán vẫn là <strong>Chờ thanh toán</strong> cho đến khi hệ thống xác minh giao dịch thành công.</p><button type="button" onClick={() => { setDone(paymentInfo.orderCode); setPaymentInfo(null); }} className="mt-6 min-h-11 w-full bg-primary px-7 py-3 text-xs uppercase tracking-[0.15em] text-primary-foreground sm:w-auto">Tôi đã chuyển khoản</button><div><button type="button" onClick={() => setPaymentInfo(null)} className="mt-4 min-h-10 px-2 text-xs text-silver underline underline-offset-4">Quay lại</button></div></main><SiteFooter /></div>;

  if (done) return <div className="min-h-screen"><SiteNav /><main className="mx-auto max-w-2xl px-6 pb-24 pt-32 text-center sm:px-12"><p className="eyebrow">Đơn hàng đã được tạo</p><h1 className="mt-3 text-4xl leading-tight sm:text-5xl">Cảm ơn bạn<span className="text-primary">!</span></h1><p className="mt-4 leading-7 text-beige">Mã đơn hàng của bạn là <span className="break-words text-primary">{done}</span>.</p><div className="mt-6 border border-border bg-card p-4 text-left text-sm leading-6 sm:p-5"><p>Đơn hàng đã được ghi nhận. Trạng thái thanh toán sẽ chỉ chuyển sang <strong>Đã thanh toán</strong> sau khi giao dịch được xác minh.</p></div><div className="mt-8 flex flex-col gap-3 sm:flex-row sm:justify-center"><Link to="/track-order" className="inline-flex min-h-11 items-center justify-center border border-primary px-7 py-3 text-xs uppercase tracking-[0.15em] text-primary">Tra cứu đơn hàng</Link><Link to="/shop" className="inline-flex min-h-11 items-center justify-center bg-primary px-7 py-3 text-xs uppercase tracking-[0.15em] text-primary-foreground">Tiếp tục mua sắm</Link></div></main><SiteFooter /></div>;

  return <div className="min-h-screen"><SiteNav /><main className="mx-auto max-w-5xl px-6 pb-24 pt-28 sm:px-12"><h1 className="text-4xl leading-tight sm:text-5xl">Thanh toán</h1>{items.length === 0 ? <p className="mt-8 leading-7 text-beige">Giỏ hàng trống. <Link to="/shop" className="text-primary">Chọn sản phẩm</Link></p> : <form className="mt-10 grid min-w-0 gap-6 lg:grid-cols-[minmax(0,1.3fr)_minmax(280px,0.7fr)] lg:gap-10" onSubmit={async (event) => { event.preventDefault(); if (submitting) return; setSubmitting(true); const form = new FormData(event.currentTarget); try { const result = await createOrder({ data: { customerName: String(form.get("name") || ""), phone: String(form.get("phone") || ""), email: String(form.get("email") || ""), address: String(form.get("address") || ""), city: String(form.get("city") || ""), district: String(form.get("district") || ""), paymentMethod, items: items.map((item) => ({ productId: item.productId, size: item.size, color: item.color, quantity: item.qty })) } }); clear(); if (paymentMethod === "vietqr") setPaymentInfo({ orderCode: result.orderCode, total: result.total, qrUrl: createVietQrUrl(result.total, result.orderCode) }); else if (paymentMethod === "momo") { const response = await fetch("/api/momo/create", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ orderCode: result.orderCode }) }); const payload = await response.json() as { success?: boolean; error?: string; payment?: { payUrl?: string } }; if (!response.ok || !payload.success || !payload.payment?.payUrl) throw new Error(payload.error || "Không thể khởi tạo thanh toán MoMo."); window.location.assign(payload.payment.payUrl); } else setDone(result.orderCode); } catch (error) { alert(error instanceof Error ? error.message : "Không thể tạo đơn hàng."); } finally { setSubmitting(false); } }}><div className="min-w-0 space-y-4"><Field label="Họ và tên" name="name" /><Field label="Số điện thoại" name="phone" type="tel" /><Field label="Email (không bắt buộc)" name="email" type="email" /><Field label="Địa chỉ giao hàng" name="address" /><div className="grid min-w-0 gap-4 sm:grid-cols-2"><Field label="Tỉnh / Thành phố" name="city" /><Field label="Quận / Huyện" name="district" /></div><p className="pt-4 text-xs uppercase leading-5 tracking-[0.2em] text-silver">Phương thức thanh toán</p><div className="space-y-3"><PaymentOption method="cod" current={paymentMethod} onChange={setPaymentMethod} title="Thanh toán khi nhận hàng (COD)" description="Thanh toán khi nhận hàng" /><PaymentOption method="vietqr" current={paymentMethod} onChange={setPaymentMethod} title="Thanh toán qua VietQR" description="Quét mã QR để thanh toán" /><PaymentOption method="momo" current={paymentMethod} onChange={setPaymentMethod} title="Thanh toán qua MoMo" description="Chuyển sang MoMo để hoàn tất thanh toán" /></div></div><aside className="h-fit min-w-0 border border-border bg-card p-5 sm:p-6 lg:sticky lg:top-24"><p className="eyebrow">Đơn hàng</p><div className="mt-4 space-y-3 text-sm">{items.map((item) => <div key={`${item.productId}-${item.size}-${item.color}`} className="grid min-w-0 grid-cols-[minmax(0,1fr)_auto] gap-3"><span className="min-w-0 break-words text-beige leading-6">{item.product.name} × {item.qty}<span className="block text-xs leading-5 text-silver">{item.size} · {item.color}</span></span><span className="whitespace-nowrap text-right">{formatVnd(item.product.price * item.qty)}</span></div>)}</div><div className="mt-4 flex items-start justify-between gap-4 border-t border-border pt-4 text-sm"><span className="text-silver">Vận chuyển</span><span>{shipping === 0 ? "Miễn phí" : formatVnd(shipping)}</span></div><div className="mt-3 flex items-start justify-between gap-4 font-display text-lg leading-tight"><span>Tổng</span><span className="text-primary">{formatVnd(subtotal + shipping)}</span></div><button type="submit" disabled={submitting} className="mt-6 flex min-h-11 w-full items-center justify-center bg-primary px-6 py-3 text-center text-xs uppercase tracking-[0.15em] text-primary-foreground disabled:opacity-50">{submitting ? "Đang tạo đơn..." : paymentMethod === "vietqr" ? "Tiếp tục thanh toán" : paymentMethod === "momo" ? "Thanh toán với MoMo" : "Đặt hàng COD"}</button></aside></form>}</main><SiteFooter /></div>;
}

function PaymentOption({ method, current, onChange, title, description }: { method: "cod" | "vietqr" | "momo"; current: "cod" | "vietqr" | "momo"; onChange: (method: "cod" | "vietqr" | "momo") => void; title: string; description: string }) { return <label className={`block cursor-pointer border p-4 text-sm transition ${current === method ? "border-primary text-primary" : "border-border text-beige"}`}><input type="radio" name="paymentMethod" value={method} checked={current === method} onChange={() => onChange(method)} className="sr-only" /><span className="block font-medium leading-6">{title}</span><span className="mt-1 block text-xs leading-5 text-silver">{description}</span></label>; }

function Field({ label, name, type = "text" }: { label: string; name: string; type?: string }) { return <label className="block min-w-0"><span className="text-xs uppercase leading-5 tracking-[0.2em] text-silver">{label}</span><input required={name !== "email"} name={name} type={type} className="mt-2 min-h-11 w-full min-w-0 border border-border bg-card px-4 py-3 text-sm leading-6 outline-none focus:border-primary" /></label>; }
