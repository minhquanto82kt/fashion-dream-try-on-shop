/* eslint-disable prettier/prettier */
import { createFileRoute, Link } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { SiteNav } from "@/components/site-nav";
import { SiteFooter } from "@/components/site-footer";
import { getCustomerSession } from "@/lib/auth";
import { listMyOrders, type CustomerOrder } from "@/lib/order.functions";
import { formatVnd } from "@/data/products";

export const Route = createFileRoute("/account/orders")({ component: CustomerOrdersPage });
function CustomerOrdersPage() { const [orders, setOrders] = useState<CustomerOrder[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { let active = true; const run = async () => { const session = getCustomerSession(); if (!session?.access_token) { if (active) { setError("Vui lòng đăng nhập để xem lịch sử đơn hàng."); setLoading(false); } return; } try { const result = await listMyOrders({ data: { accessToken: session.access_token, limit: 50 } }); if (active) setOrders(result.orders); } catch (err) { if (active) setError(err instanceof Error ? err.message : "Không thể tải lịch sử đơn hàng."); } finally { if (active) setLoading(false); } }; void run(); return () => { active = false; }; }, []); return <div className="min-h-screen"><SiteNav /><main className="mx-auto max-w-5xl px-6 pb-24 pt-28 sm:px-12"><nav aria-label="Breadcrumb" className="mb-8 flex gap-2 text-[10px] uppercase tracking-[0.16em] text-silver"><Link to="/account" className="hover:text-primary">Tài khoản</Link><span>/</span><span>Đơn hàng</span></nav><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Order history</p><h1 className="mt-2 text-4xl leading-tight sm:text-5xl">Đơn hàng của bạn</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-silver">Theo dõi trạng thái đơn hàng và thanh toán từ tài khoản khách hàng.</p></div><Link to="/shop" className="min-h-10 border border-primary px-4 py-2 text-xs uppercase tracking-[0.14em] text-primary hover:bg-primary hover:text-primary-foreground">Tiếp tục mua sắm</Link></div>{loading ? <div className="mt-8 border border-border bg-card p-8 text-center text-sm text-silver">Đang tải đơn hàng…</div> : error ? <div className="mt-8 border border-destructive/40 bg-destructive/5 p-6 text-sm leading-6 text-destructive">{error}<div className="mt-4"><Link to="/account" className="text-primary underline underline-offset-4">Đăng nhập</Link></div></div> : orders.length === 0 ? <div className="mt-8 border border-border bg-card p-8 text-center"><p className="text-sm text-beige">Bạn chưa có đơn hàng nào.</p><Link to="/shop" className="mt-5 inline-flex min-h-10 items-center bg-primary px-5 py-2 text-xs uppercase tracking-[0.14em] text-primary-foreground">Khám phá sản phẩm</Link></div> : <div className="mt-8 space-y-3">{orders.map((order) => <Link key={order.id} to="/account/orders/$id" params={{ id: order.id }} className="block border border-border bg-card p-5 transition-colors hover:border-primary sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-[0.15em] text-primary">{order.order_code}</p><p className="mt-2 text-xs text-silver">{new Date(order.created_at).toLocaleString("vi-VN")}</p></div><div className="text-right"><p className="font-display text-lg text-primary">{formatVnd(order.total)}</p><p className="mt-1 text-[10px] uppercase tracking-[0.14em] text-silver">{order.order_status} · {order.payment_status}</p></div></div></Link>)}</div>}</main><SiteFooter /></div>; }
